# Проект «Web-Larёk»

> Учебный проект, выполненный в рамках курса «Фронтенд-разработчик» от Яндекс Практикума по разработке интернет-магазина на TypeScript с архитектурой MVP.

### Задача проекта:
- Спроектировать архитектуру приложения, разделив хранение данных, отображение интерфейса и логику взаимодействия.
- Реализовать каталог, корзину и оформление заказа на основе готовой вёрстки.
- Типизировать данные и настроить обмен с сервером.
- Описать назначение классов и принципы их взаимодействия в документации.

## Технологический стек:
![TypeScript](https://img.shields.io/badge/TypeScript-3178C6?style=for-the-badge&logo=typescript&logoColor=white)
![HTML5](https://img.shields.io/badge/HTML5-E34F26?style=for-the-badge&logo=html5&logoColor=white)
![SCSS](https://img.shields.io/badge/SCSS-CC6699?style=for-the-badge&logo=sass&logoColor=white)
![Vite](https://img.shields.io/badge/Vite-646CFF?style=for-the-badge&logo=vite&logoColor=white)

### Архитектура и подходы
![OOP](https://img.shields.io/badge/ООП-007ACC?style=for-the-badge)
![MVP](https://img.shields.io/badge/MVP-764ABC?style=for-the-badge)
![Event Driven](https://img.shields.io/badge/Event_Driven-00897B?style=for-the-badge)

## Описание проекта

**«Web-Larёk»** — интернет-магазин товаров для веб-разработчиков. Пользователь может изучить каталог, посмотреть подробности товара, собрать корзину и оформить заказ, указав способ оплаты, адрес доставки и контактные данные.

Вёрстка, базовые утилиты, брокер событий и API-клиент предоставлены в заготовке. Моя работа — проектирование архитектуры и реализация логики приложения.

## Реализовала:

- Спроектировала приложение по MVP и описала ответственность классов и их взаимодействие.
- Создала модели каталога, корзины и покупателя, отделив работу с данными от отображения интерфейса.
- Описала интерфейсы и типы товаров, данных покупателя, запросов и ответов сервера.
- Разработала `ApiService` для получения каталога и отправки заказа с использованием базового API-клиента.
- Создала компоненты представления для каталога, карточек, корзины, форм и модальных окон.
- Связала модели и представления через события, разместив логику презентера в `main.ts`.
- Реализовала добавление и удаление товаров, расчёт общей стоимости и обновление счётчика корзины.
- Исключила повторное добавление товара и заблокировала покупку товаров без цены.
- Реализовала двухэтапное оформление заказа с проверкой обязательных полей.
- Настроила очистку корзины и данных покупателя после успешного ответа сервера.

## Результат:

- Научилась проектировать архитектуру приложения до написания кода и описывать её в документации.
- Закрепила принципы ООП: разделение ответственности, инкапсуляцию, наследование и композицию.
- Освоила разделение приложения на модели, представления и презентер.
- Научилась связывать компоненты через брокер событий, сохраняя независимость моделей от DOM.
- Получила опыт типизации данных и взаимодействия с API на TypeScript.
- Отработала создание пользовательского сценария от выбора товара до подтверждения заказа.


<details>
  <summary><h2>Структура проекта</h2>
  </summary>
  
```text
.
├── src/
│   ├── components/
│   │   ├── base/            # базовые классы компонентов, API и событий
│   │   ├── Communication/   # получение каталога и отправка заказа
│   │   ├── Models/          # модели каталога, корзины и покупателя
│   │   └── View/            # компоненты интерфейса
│   ├── types/               # интерфейсы и типы данных
│   ├── utils/               # утилиты и константы
│   ├── scss/                # общие стили, переменные и миксины
│   ├── common.blocks/       # стили блоков интерфейса
│   ├── vendor/              # шрифты и сторонние стили
│   └── main.ts              # точка входа и логика презентера
├── public/                  # статические ресурсы
├── index.html               # страница и HTML-шаблоны
├── vite.config.ts           # настройки Vite
├── tsconfig.json            # настройки TypeScript
├── package.json             # зависимости и команды
└── README.md                # документация проекта
```
</details>

## Запуск

1. В каталоге проекта установите зависимости:

```bash
npm install
```

2. Запустите приложение:

```bash
npm run dev
```

<details>
  <summary><h2>Подробное описание архитектуры</h2>
  </summary>
  
Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

`Model` - слой данных, отвечает за хранение и изменение данных.  
`View` - слой представления, отвечает за отображение данных на странице.  
`Presenter` - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

---

## Базовые компоненты

### `Класс Component` — базовый класс для всех компонентов интерфейса. Является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

***Конструктор:***  
- `constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

***Поля класса:***  
- `container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

***Методы класса:***  
- `render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
- `setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

---
### `Класс Api` — содержит в себе базовую логику отправки запросов.

***Конструктор:***  
- `constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

***Поля класса:***  
- `baseUrl: string` - базовый адрес сервера  
- `options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

***Методы:***  
- `get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
- `post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
- `handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

---
### `Класс EventEmitter` — брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

***Конструктор класса не принимает параметров.***

***Поля класса:***  
- `_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

***Методы класса:***  
- `on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
- `emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
- `trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

---
### `Класс ApiService` — отвечает за взаимодействие с сервером.

***Конструктор:***  
- `constructor(data: IApi)` - принимает объект с данными с сервера.

***Поля класса:***  
- `data: IApi`- объект с данными.

***Методы класса:***  
- `getProduct(): Promise<TGet>` - делает GET запрос на /product и возвращает список товаров.  
- `postProduct(data: TPostRequest): Promise<TPostResponse>` - делает POST запрос на /order и принимает: данные покупателя и список товаров.

---
---
## Интерфейсы дынных

### `interface IProduct` — отражает данные товара.
`id: string` — идентификатор товара.  
`description: string` — описание товара.  
`image: string` — изображение товара.  
`title: string` — название товара.  
`category: string` — категория товара.  
`price: number | null` — цена товара или её отсутствие(бесценный товар).

### `interface IBuyer` — отражает данные о покупателе. 
`payment: TPayment` — способ оплаты, тип которого `TPayment`, включающий несколько вариантов оплаты: `'card' | 'cash' | null` — карта, или наличные, или ничего не выбрано.  
`email: string` — email покупателя.  
`phone: string` — номер телефона покупателя.  
`address: string` — адресс доставки покупателя.

---
---
## Model

### `Класс CatalogProduct` — реализует весь каталог товаров.

***Конструктор:***  
- `constructor(products: IProduct[], product: IProduct | null)` — передаются данные товара.

***Поля класса:***
- `products: IProduct[]` — каталог товаров на основе интерфейса `IProduct`.  
- `product: IProduct | null` - поле для получения выбранной карточки.

***Методы класса:***  
- `saveCatalog(catalog: IProduct[])` — сохраняет массив каталога товаров.  
- `getCatalog(): IProduct[]` — получает массив каталога товаров.  
- `getProductById(id: string): IProduct` — получает выбранный товар по его id.  
- `saveSelectedProduct(item: IProduct)` — сохраняет выбранный товар для подробного отображения.  
- `getSelectedProduct(): IProduct` — получает выбранный товар для подробного отображения.

---

### `Класс Basket` — реализует товары в корзине с возможностью выполнять различные действия с ними(считать, удалять, добавлять).

***Конструктор класса не принимает параметров.***

***Поля класса:***
- `catalogBasket: IProduct[]` — массив товаров корзины.

***Методы класса:***
- `getCatalog(): IProduct[]` - получить массив товаров корзины.  
- `addProduct(product: IProduct)` — добавить товар в корзину по id.  
- `removeProduct(id: string)` — удалить товар из корзины.  
- `getAllPrice(): number` — получение стоимость всех товаров в корзине.  
- `getAllProducts(): number` - получение количества товаров в корзине.  
- `checkProductBasket(id: string): boolean` - проверка наличия товара в корзине по id.
- `clear()` - очищает корзину.

---
### `Класс Buyer` — реализует информацию о покупателе.

***Конструктор класса не принимает параметров.***

***Поля класса:***  
- `payment: TPayment`- способ оплаты.  
- `address: string` - адрес покупателя.  
- `phone: string` - телефон покупателя.  
- `email: string` - email покупателя.

***Методы класса:***  
- `saveDataBuyer(data: IBuyer)` - сохранение данных: способа оплаты, номера телефона, адреса, почты.  
- `getDataBuyer(): IBuyer` - получение данных пользователя.  
- `clearDataBuyer(): void` - очищение данных пользователя после совершения оплаты.  
- `validateDataBuyer(): {isValid: boolean, errors: TBuyerErrors}` - проверка корректности заполнения данных пользователем.
  
---
---
## View

### `Класс BasketContainer` — управляет отображением корзины: список товаров, итоговая сумма и действия пользователя.

***Конструктор:***
- `constructor(protected events: IEvents, template: string)` - принимает события приложения и HTML-шаблон контейнера корзины.

***Поля класса:***  
- `listItems: HTMLElement` - контейнер списка товаров в корзине.  
- `orderButton: HTMLButtonElement` - кнопка оформления заказа.  
- `totalPrice: HTMLElement` - отображение общей стоимости товаров в корзине.

***Методы класса:***  
- `set items(items: HTMLElement[])` - обновляет список товаров в корзине.  
- `set price(value: number)` - устанавливает общую стоимость товаров в корзине.

---
### `Класс Card` — базовый класс карточек товаров (каталог, корзина, превью). Управляет общими UI-элементами карточек.

***Конструктор:***  
- `constructor(template: string)` - принимает HTML-шаблон карточки.

***Поля класса:***  
- `cardTitle: HTMLElement` - название товара.  
- `cardPrice: HTMLElement` - цена товара.  
- `cardId!: string` - идентификатор товара.

***Методы класса:***  
- `set price(value: number | null)` - устанавливает цену товара или состояние "нет цены".  
- `set id(value: string)` - сохраняет идентификатор товара.  
- `set title(value: string)` - устанавливает название товара.

---
### `Класс CardBasket` — карточка товара в корзине.

***Конструктор:***  
- `constructor(container: HTMLElement, handlers: {onDelete: () => void})` - принимает DOM-контейнер и обработчик удаления товара.

***Поля класса:***  
- `cardIndex: HTMLElement` - порядковый номер товара в корзине.  
- `cardDeleteButton: HTMLButtonElement` - кнопка удаления товара.

***Методы класса:***  
- `set index(value: number)` - устанавливает порядковый номер товара.

---
### `Класс CardCatalog` — карточка товара в каталоге.

***Конструктор:***  
- `constructor(container: HTMLElement, handlers: {onClick: () => void})` - принимает контейнер и обработчик открытия превью.

***Поля класса:***  
- `cardCatalogButton: HTMLButtonElement` - кликабельная область - вся карточка.  
- `cardCategory: HTMLElement` - категория товара.  
- `imageElement: HTMLImageElement` - изображение товара.

***Методы класса:***  
- `set category(value: string)` - устанавливает категорию товара и стилизует её.  
- `set image(value: string)` - устанавливает изображение товара.

---
### `Класс CardPreview` — карточка товара в режиме полного просмотра.

***Конструктор:***  
- `constructor(protected events: IEvents, container: HTMLElement)` - принимает события и DOM-контейнер.

***Поля класса:***  
- `cardText: HTMLElement` - описание товара.  
- `cardCategory: HTMLElement` - категория товара.  
- `addToBasketButton: HTMLButtonElement` - кнопка добавления/удаления из корзины.  
- `imageElement: HTMLImageElement` - изображение товара.

***Методы класса:***  
- `set description(value: string)` - устанавливает описание товара.  
- `set category(value: string)` - устанавливает категорию товара.  
- `set price(value: number | null)` - устанавливает цену и состояние доступности покупки.  
- `set image(value: string)` - устанавливает изображение товара.  
- `set buttonState(inBasket: boolean)` - изменяет текст кнопки в зависимости от состояния корзины.

---
### `Класс Form` — базовый класс форм. Управляет общими UI-элементами форм.

***Конструктор:***  
- `constructor(protected events: IEvents, container: HTMLElement)` - принимает события и контейнер формы.

***Поля класса:***  
- `errorElement: HTMLElement` - блок отображения ошибок.  
- `continueButton: HTMLButtonElement` - кнопка отправки формы.

***Методы класса:***  
- `set valid(value: boolean)` - включает/выключает кнопку отправки.  
- `set errors(value: string[])` - отображает ошибки формы.

---
### `Класс OrderForm` — форма оформления заказа (способ оплаты и адрес).

***Конструктор:***  
- `constructor(events: IEvents, container: HTMLElement)` - принимает события и DOM-контейнер формы заказа.

***Поля класса:***  
- `cardButton: HTMLButtonElement` - выбор оплаты картой.  
- `cashButton: HTMLButtonElement` - выбор оплаты наличными.  
- `addressInput: HTMLInputElement` - поле адреса.

***Методы класса:***  
- `set address(value: string)` - устанавливает адрес доставки.  
- `set payment(value: TPayment | "")` - устанавливает выбранный способ оплаты.

---
### `Класс ContactsForm` — форма контактных данных пользователя.

***Конструктор:***  
- `constructor(events: IEvents, container: HTMLElement)` - принимает события и DOM-контейнер.

***Поля класса:***  
- `emailInput: HTMLInputElement` - поле email.  
- `phoneInput: HTMLInputElement` - поле телефона.

***Методы класса:***  
- `set email(value: string)` - устанавливает email.  
- `set phone(value: string)` - устанавливает телефон.

---
### `Класс Gallery` — отображает список товаров каталога.

***Конструктор:***  
- `constructor(container: HTMLElement)` - принимает контейнер галереи.

***Методы класса:***  
- `set catalog(items: HTMLElement[])` - устанавливает список карточек товаров.

---
### `Класс Header` — управляет корзиной и счетчиков в шапке сайта.

***Конструктор:***  
- `constructor(protected events: IEvents, container: HTMLElement)` -

***Поля класса:***  
- `basketButton: HTMLButtonElement` - принимает события и контейнер корзины.  
- `counterElement: HTMLElement` - счётчик товаров.

***Методы класса:***  
- `set counter(value: number)` - обновляет количество товаров в корзине.

---
### `Класс Modal` — управляет модальными окнами.

***Конструктор:***  
- `constructor(protected events: IEvents, container: HTMLElement)` - принимает события и контейнер модального окна.

***Поля класса:***  
- `contentElement: HTMLElement` - содержимое модального окна.  
- `closeButton: HTMLButtonElement` - кнопка закрытия.

***Методы класса:***  
- `set content(content: HTMLElement)` - устанавливает содержимое модального окна.  
- `open()` - открывает модальное окно.  
- `close()` - закрывает модальное окно.

---
### `Класс OrderSuccess` — экран успешного оформления заказа.

***Конструктор:***  
- `constructor(protected events: IEvents, container: HTMLElement)` - принимает события и контейнер.

***Поля класса:***  
- `buttonSuccessClose: HTMLButtonElement` - кнопка закрытия окна успешного заказа.  
- `orderSuccessDescription: HTMLElement` - текст результата заказа.

***Методы класса:***  
- `set amount(value: number)` - отображает сумму заказа.

---
---
## Presenter

Код презентера не будет выделен в отдельный класс, а будет содержаться в основном скрипте приложения `main.ts`. Обмен данными между слоями будет реализован с помощью брокера событий.

### Модели данных:

- `'catalog: updated'` - изменение каталога товаров.  
- `'product: selected'` - изменение выбранного для просмотра товара.  
- `'basket: changed'` - изменение содержимого корзины.  
- `'buyer:change'` - изменение данных покупателя.

### Представления:

- `'card: previewCard'` - выбор карточки для просмотра.  
- `'card: addToBasket'` - нажатие кнопки покупки товара.  
- `'basket: deleteCard'` - нажатие кнопки удаления товара из корзины.  
- `'basket: makeAnOrder'` - нажатие кнопки оформления заказа.  
- `'orderForm: submit'` - нажатие кнопки перехода ко второй форме оформления заказа.  
- `'contactsForm: submit'` - нажатие кнопки оплаты/завершения оформления заказа.  
- `'input: address'` - изменение данных в поле адреса.  
- `'input: phone'` - изменение данных в поле телефона.  
- `'button: cash'` - выбор кнопки оплаты наличными.  
- `'button: card'` - выбор кнопки оплаты по карте.  
- `'input: email'` - изменение данных в поле почты.  
- `'order: submit'` — отправка формы заказа и данных покупателя.  
- `'basket: open'` - нажатие кнопки открытия корзины.  
- `'modal: open'` - нажатие кнопки открытия модального окна.  
- `'modal: close'` - нажатие кнопки закрытия модального окна.  
- `'order: close'` - нажатие кнопки перехода к новым покупкам.

</details>


## Полезные команды
| Команда | Назначение |
| --- | --- |
| `npm run dev` | запуск сервера разработки |
| `npm run build` | проверка TypeScript и сборка проекта в `dist` |
| `npm run preview` | локальный просмотр готовой сборки |
